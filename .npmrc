# npm configuration
# The overrides in package.json handle deprecated packages by forcing modern alternatives
legacy-peer-deps=false
fund=false
# Keep audit enabled for security, but overrides handle deprecated package warnings

